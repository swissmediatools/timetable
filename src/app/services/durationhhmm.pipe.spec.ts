import { DurationhhmmPipe } from './durationhhmm.pipe';

describe('DurationhhmmPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationhhmmPipe();
    expect(pipe).toBeTruthy();
  });
});
